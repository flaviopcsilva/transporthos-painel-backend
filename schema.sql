create table clientes2(
id serial primary key,
  data date,
  hora time,
  cliente text,
  quantidade integer,
  di text,
  dta text,
  tipo_de_carga text,
  processo text,
  origem text,
  destino text,
  motorista text,
  pl_cavalo text,
  pl_carreta text,
  conferente text,
  ajudantes text,
  status text
);